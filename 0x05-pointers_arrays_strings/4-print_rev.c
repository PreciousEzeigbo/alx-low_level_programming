#include "main.h"
/**
 * print_rev - prints string in reverse
 * @s: functions parameter
 * Return: 0
 */
void print_rev(char *s)
{
	int start = 0;

	for (i = count ; i >= 0 ; i--)
		_putchar(s[i]);
	_putchar('\n');
}
